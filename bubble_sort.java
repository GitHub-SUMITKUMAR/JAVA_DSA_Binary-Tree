package com.company;

import java.util.Scanner;
/*
public class bubblesort {
    int arr[];
    bubblesort(int size){
        arr=new int[size];

    }
    public void sortass(){
        int n= arr.length;
        int t;
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length-i-1; j++) {
                if(arr[j]>arr[j+1]) {
                    t = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = t;
                }
            }
        }
    }
    public void sortdesc(){
        int n= arr.length;
        int t;
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length-i-1; j++) {
                if(arr[j]<arr[j+1]) {
                    t = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = t;
                }
            }
        }
    }
    public void dispaly(){
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i]+" ");
        }
        System.out.println();
    }
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter the size of array");
        bubblesort obj=new bubblesort(sc.nextInt());
        for (int i = 0; i < obj.arr.length; i++) {
            obj.arr[i]= sc.nextInt();
        }
        obj.dispaly();
        obj.sortass();
        obj.dispaly();
        obj.sortdesc();
        obj.dispaly();
    }
}

 */
